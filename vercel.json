{
  "buildCommand": "pnpm db:deploy && pnpm --filter @workspace/ai-tools-directory build && pnpm --filter @workspace/api-server build",
  "outputDirectory": "apps/web/dist/public",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" }
  ]
}
