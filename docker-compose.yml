services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: theaitoolbox
      POSTGRES_PASSWORD: theaitoolbox
      POSTGRES_DB: theaitoolbox
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
